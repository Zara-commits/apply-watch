# apply-watch
script notifies you once the Google job posting get fixed (gets the apply button added)
if you want to get notified once the google new grad swe role apply button appear scan the QR code and get the Ntfy app
